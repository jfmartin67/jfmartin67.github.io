# Optional — only needed to preview the site locally with:
#   bundle install && bundle exec jekyll serve
# GitHub Pages builds the site without this file.
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "jekyll-seo-tag"

# Lock to versions compatible with the GitHub Pages build environment.
gem "webrick", "~> 1.8"
